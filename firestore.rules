rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Emulator-only permissive rules. Do NOT deploy to prod.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
